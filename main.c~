#include <stdio.h> 

#ifdef __DEBUG__
#define HelloWorld 1
#endif

#ifdef __OTHR___
#define GoodbyeWorld 1
#endif

int main()
{
  if (HelloWorld && GoodbyeWorld)
    {
      printf("hello world\n\r");
    }
}
